# ── Test Suite Configuration ──
# Dev server API is only accessible via SSH tunnel or from within the server
# For remote tests, we SSH and curl localhost:3001
BASE_URL=http://localhost:3001

# Admin user (also has vendor profile)
ADMIN_EMAIL=jermaine@agelessliterature.com
ADMIN_PASSWORD=password

# Vendor user (same as admin in this case — jermaine has vendor profile id=37)
VENDOR_EMAIL=jermaine@agelessliterature.com
VENDOR_PASSWORD=password

# Regular buyer (will be created by the test suite)
TEST_EMAIL=testrunner@agelessliterature.com
TEST_PASSWORD=TestRunner123!

# SSH access to dev server (tests run from local machine via SSH)
SSH_KEY=~/.ssh/AgelessLiteratureMasterKey.pem
SSH_HOST=bitnami@3.239.234.232
